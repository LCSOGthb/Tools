build:
    image: default-jammy

    environment:
        node: 22

    nodes:
        analysis:
            dependencies:
                override:
                    - npm install -g pnpm@10

            tests:
                override:
                    - cd artifacts/personal-tool-console
                    - pnpm install --frozen-lockfile
                    - pnpm run build
