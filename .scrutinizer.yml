checks:
    javascript: true

build:
    image: default-jammy
    nodes:
        analysis:
            commands:
                - corepack enable
                - corepack prepare pnpm@latest-11 --activate
                - pnpm install --frozen-lockfile
                - pnpm exec js-scrutinizer-run
