checks:
    javascript: true

before_commands:
    - corepack enable
    - corepack prepare pnpm@latest-11 --activate
    - pnpm install --frozen-lockfile

build:
    image: default-jammy
    nodes:
        analysis:
            environment:
                node: 20
            tests:
                override:
                    - js-scrutinizer-run
