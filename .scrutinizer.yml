checks:
    javascript: true

build:
    image: default-jammy
    nodes:
        analysis:
            commands:
                - checkout-code ~/build
                - corepack enable
                - corepack prepare pnpm@latest-11 --activate
                - pnpm install --frozen-lockfile
                - pnpm exec js-scrutinizer-run
