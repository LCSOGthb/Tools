checks:
    javascript: true

build:
    image: default-jammy
    nodes:
        analysis:
            environment:
                node: 20
            commands:
                - corepack enable
                - corepack prepare pnpm@latest-11 --activate
                - pnpm install --frozen-lockfile
                - pnpm exec js-scrutinizer-run
