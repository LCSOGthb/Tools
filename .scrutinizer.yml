checks:
    javascript: true

build:
    image: default-jammy

    nodes:
        analysis:
            environment:
                node: 20

            tests:
                override:
                    - corepack enable
                    - pnpm install --frozen-lockfile
                    - pnpm build
